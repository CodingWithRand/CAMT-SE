<%- include("partials/header") %>
<div class="post">
    <h1 class="text-4xl font-bold"><%= post.title %></h2>
    <% post.body.split("\n").forEach((line) => { %>
        <p class="my-4"><%= line %></p>
    <% }) %>
</div>
<%- include("partials/footer") %>